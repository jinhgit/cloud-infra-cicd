apiVersion: v1
kind: Service
metadata:
  name: fe
  namespace: cloud-infra
  labels:
    app: fe
spec:
  type: ClusterIP
  selector:
    app: fe
  ports:
    - name: http
      port: 80
      targetPort: http
